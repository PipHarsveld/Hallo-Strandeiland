<ul class="themeFilters">
    {{#each themeFilters}}
        <div>
            <input type="checkbox" name="theme" id="{{id}}" value="{{value}}">
            <label for="{{id}}">
                <div>{{label}}</div>
            </label>
        </div>
    {{/each}}
</ul>